# Copy to .env.local and fill in. Never commit real secrets.

# Supabase (see Supabase project settings)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Admin workbook: shared passphrase (8+ characters). Same value in Vercel env for production.
# Admins type this each time they open Admin → Console.
SYSTEM_CONSOLE_PASSPHRASE=
